gia na exoume thn teleytai ekdosh tou giorgou: pull

git branch

git fetch origin

git pull origin main

php artisan serve

prin apo ayta kaname cd... kai git clone... mia fora ginetai ayto

gia push:

git branch

git checkout -b my-branch

git checkout -b user-auth

git status

git add .

git commit -m "Περιγραφή αλλαγών"

git push origin my-branch

Δες τις αλλαγές σου (η «γραμμούλα»)

Στο project φάκελο:

git status


Θα δεις:

ποια αρχεία πείραξες

ότι είναι modified

3️⃣ Δες ακριβώς τις γραμμές που πρόσθεσες
git diff


🔹 Εκεί φαίνονται οι πράσινες/κόκκινες γραμμές
🔹 Αυτό είναι ακριβώς που θες για συνεργασία

Αν δουλεύεις σε VS Code:

Άνοιξε το αρχείο

Θα δεις πράσινη γραμμή αριστερά δίπλα στις αλλαγές ✔
